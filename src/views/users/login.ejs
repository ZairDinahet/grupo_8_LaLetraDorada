<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link rel="stylesheet" href="/css/login.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;500;700&display=swap" rel="stylesheet">
    <link rel="icon" href="/design/la-letra-dorada-icon1.png">
    <title>Login - La Letra Dorada</title>
</head>

<body>

  <%- include('../partials/header.ejs') %>
    <hr>
    <main>
        <div class="main-login-container">

          <div class="information-login-content">  
            <h1>Bienvenido</h1>
            <p>¡Nos alegra que hayas vuelto!</p>
          </div>
          
          <div class="form-login-content">
            <div class="sub-form-content">

              <h2>Inicia Sesión</h2>

              <form class="login-form-content" action="/user/login" method="post">

                <div class="field-form">
                    <label for="email">Correo Electronico</label>
                    <div class="input-icon">
                        <i class="fa-regular fa-envelope"></i>
                        <input type="text" name="email" id="email" placeholder="Ingresa tu Email" value="<%= (locals.oldData && oldData.email) ? oldData.email : null %>" >
                        
                      </div>

                      <% if (locals.errors && errors.email) { %>
                        <div class="text-danger"><%= errors.email.msg %></div>  
                        <% } else { %>
                      <div class="text-danger" id="errorEmail"><p></p></div>  
                      <% } %>

                </div>

                <div class="field-form">
                  <label for="password">Contraseña</label>
                  <div class="input-icon">
                      <i class="fa-solid fa-lock"></i>
                      <input type="password" name="password" id="password" placeholder="Ingresa tu Contraseña">
                  </div>

                    <% if (locals.errors && errors.password) { %>
                      <div class="text-danger"><%= errors.password.msg %></div>  
                    <% } else { %>
                      <div class="text-danger" id="errorPass"><p></p></div>  
                    <% }%>

                    <a href="">¿Has olvidado tu contraseña?</a>
                </div>

                <div class="checkbox"> <label> <input type="checkbox" name="Recordarme"> Recordarme</label> </div>

                <button class="form-button-access" type="submit"> Acceder </button>

                <span>¿Aún no tienes una cuenta?<a href="/user/register"> Registrate</a></span>

              </form>
            </div>
          </div>
        </div>
    </main>
    <hr>
    <%- include('../partials/footer.ejs') %>
    <script src="/js/login.js"></script>
</body>

</html>